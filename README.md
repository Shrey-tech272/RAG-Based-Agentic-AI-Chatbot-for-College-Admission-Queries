# RAG-Based-Agentic-AI-Chatbot-for-College-Admission-Queries
An n8n automation that ingests a PDF, chunks and embeds it via Ollama (nomic-embed-text) into Pinecone, then answers user queries using LLaMA 3.2 with context-aware retrieval — a fully local RAG pipeline.
